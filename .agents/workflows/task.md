# Task: Introduce Agent Workflows to TizenClaw-Assets

## Objective
Update the `tizenclaw-assets` project to integrate the development guidelines and `AGENTS.md` from the `tizenclaw` project.
Ensure that future development in this repository strictly adheres to the 6-phase methodology: 
1. Plan
2. Design
3. Develop
4. Build & Deploy
5. Test & Review
6. Commit

## Scope
1. **Plan**: Define the task and high-level requirements.
2. **Design**: Establish mapping of `tizenclaw` specific paths and names to `tizenclaw-assets`. Identify which workflow documents need customization.
3. **Develop**: Adapt `AGENTS.md` and `.agents/workflows/*` to correctly represent `tizenclaw-assets`.
4. **Build & Deploy**: Verify the documents are correctly structured and formatted.
5. **Test & Review**: Review the documents against the C++ standards and build mechanisms of `tizenclaw-assets`.
6. **Commit**: Add and commit the updated guidelines using the defined commit standards.
